import React from 'react';

const DefaultPage: React.FC = () => {
    return (
        <div>
            <h1>Welcome to the Default Page</h1>
            <p>This is a simple default page created using TypeScript and React.</p>
        </div>
    );
};

export default DefaultPage;